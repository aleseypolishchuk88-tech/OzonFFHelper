# AGENTS.md — D:\OzonFFHelper

> Streamlit-инструмент поставок Ozon FBO: опись от фулфилмента → грузоместа в Ozon API →
> ZIP PDF-этикеток (имена PAC) + лист «Оприходование» + дубль поставки в ФФ-систему migfull
> (RPA на Playwright). Основной агент — Claude Code; Codex — дублёр.

- Ядро: `core/` — `opis_parser.py` (парсинг описи), `cargoes.py` (грузоместа), `ozon_api.py` (supply-order/cargoes/cargoes-label), `orchestrator.py`, `oprihodovanie.py`, `migfull_rpa.py`.
- Правила домена: PAC = короб; состав грузоместа по числу ШК; ФФ опознаёт товар ТОЛЬКО по ШК (`OZN<sku>` = баркод). Справочник: память `ozon-ff-cargo-rules`, `ozon-fbo-cargoes-reference` (в бэкапе `_claude/memory/`).
- Грабли API: редактировать состав ГМ нельзя — только удалить+пересоздать (cargo_id меняются!); имена складов сравнивать без учёта регистра.
- Открытый хвост: Шаг 2б — запись грузомест (`create_cargoes`) заглушка; этикетки шаги 4–5 — доделать.
- Разработка по шагам, каждый шаг обкатывается перед следующим (правило пользователя).
- Ключи Ozon — в `D:\Клод Код\.env`.

## 🌍 ВНИМАНИЕ: этот репозиторий ПУБЛИЧНЫЙ
`github.com/aleseypolishchuk88-tech/OzonFFHelper` — **открыт всему интернету, и это намеренно**:
`updater.py` тянет обновления через `raw.githubusercontent.com` без авторизации, поэтому
приватным его делать НЕЛЬЗЯ (сломается автообновление у менеджеров ФФ).

**Следствие: всё, что сюда закоммичено, видно любому.** Перед каждым коммитом — `git status`
и глазами проверить, что не попало: `.env` (он и так лежит в `D:\Клод Код`, не здесь) ·
реальные ключи в `.env.example` (там должны быть ТОЛЬКО плейсхолдеры) · описи и выгрузки
поставок с реальными данными · скриншоты ЛК · `*.session`.
Проверено 20.07.2026 — секретов в репозитории нет, `.env.example` пустой. Так и держать.

**Агент-дублёр (Codex):** работать в отдельной ветке, `master` ведёт основной агент.
Правила по остальным репозиториям — `D:\Клод Код\AGENTS.md`.
